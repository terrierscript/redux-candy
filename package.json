{
  "name": "redux-candy",
  "version": "0.0.5",
  "main": "lib/index.js",
  "description": "Generate redux updeep based reducer and actions.",
  "files": [
    "lib/*"
  ],
  "author": "inuscript <inuscript@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf lib && babel src -d lib",
    "lint": "eslint src/ --fix",
    "test": "yarn lint && yarn build && mocha --compilers js:babel-register",
    "prepublish": "in-publish && yarn build || not-in-publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/inuscript/redux-candy"
  },
  "keyword": [
    "redux"
  ],
  "dependencies": {
    "redux-actions": "^1.2.0",
    "updeep": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "eslint": "^3.12.2",
    "eslint-config-standard": "^6.2.1",
    "eslint-plugin-promise": "^3.4.0",
    "eslint-plugin-standard": "^2.0.1",
    "in-publish": "^2.0.0",
    "mocha": "^3.2.0",
    "redux-act": "^1.1.0",
    "webpack": "2.2.0-rc.3"
  }
}
